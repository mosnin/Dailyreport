# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`mosnin/dailyreport`) is newly initialized and currently empty. There is no application code, build system, or test suite yet. Update this file as the project is built out.

## Development Branch Convention

Active development happens on feature branches. The initial documentation branch is `claude/add-claude-documentation-bOMls`. Push changes with:

```
git push -u origin <branch-name>
```

Always open a draft pull request after pushing a new branch.
